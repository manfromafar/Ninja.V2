all:
	(cd objects; ${MAKE})
	(cd templates; ${MAKE})
	(cd handlers; ${MAKE})
	(cd src; ${MAKE})

clean:
	(cd objects; ${MAKE} clean)
	(cd templates; ${MAKE} clean)
	(cd handlers; ${MAKE} clean)
	(cd src; ${MAKE} clean)
